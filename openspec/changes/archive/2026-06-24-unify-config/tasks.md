## 1. 配置文件更新

- [x] 1.1 更新 `.env` 文件，确保所有配置项正确且一致
- [x] 1.2 更新 `.env.example` 文件，添加所有必需配置项及注释

## 2. 前端配置改造

- [x] 2.1 修改 `vite.config.ts`，使用 `loadEnv` 从环境变量读取 `VITE_API_BASE_URL`
- [x] 2.2 为前端添加默认配置值支持

## 3. 启动脚本改造

- [x] 3.1 创建通用的 `.env` 解析函数（可复用）
- [x] 3.2 修改 `start-backend.ps1`，从 `.env` 读取 `BACKEND_PORT` 等配置
- [x] 3.3 修改 `start-frontend.ps1`，从 `.env` 读取 `VITE_API_BASE_URL` 等配置

## 4. 统一启动入口

- [x] 4.1 创建 `start.ps1` 统一启动脚本，检查依赖并启动所有服务
- [x] 4.2 创建 `stop.ps1` 统一停止脚本，停止所有服务进程
- [x] 4.3 启动脚本显示服务地址和状态信息

## 5. 验证与测试

- [x] 5.1 验证修改配置后重新启动，所有组件使用新配置
- [x] 5.2 验证删除 `.env` 后启动失败并显示友好错误
- [x] 5.3 验证从 `.env.example` 复制后可正常启动
- [x] 5.4 验证 `start.ps1` 一键启动所有服务
- [x] 5.5 验证 `stop.ps1` 一键停止所有服务
