## 1. 修复消息气泡对齐

- [x] 1.1 在 `MessageBubble.tsx` 外层容器添加 `items-start` 确保头像与气泡顶部对齐
- [x] 1.2 将用户消息的 `flex justify-end` 改为 `ml-auto` 实现正确右对齐
- [x] 1.3 用户消息气泡宽度从 85% 改为 90%，AI 消息保持 85%

## 2. 验证布局效果

- [x] 2.1 启动前端开发服务器验证用户消息右对齐效果
- [x] 2.2 验证 AI 消息左对齐和头像对齐效果
- [x] 2.3 测试不同屏幕宽度下的响应式布局
