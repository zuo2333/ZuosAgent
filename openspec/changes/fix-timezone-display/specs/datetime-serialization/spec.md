## 新增需求

### 需求:时间字符串必须包含 UTC 时区标识

系统必须在返回时间字段时使用 ISO 8601 格式，并附加 `Z` 后缀标识 UTC 时区。

#### 场景:新建会话的时间显示正确

- **当** 用户创建一个新会话
- **那么** 前端必须显示"Just now"或"1 min ago"，而不是"8h ago"

#### 场景:历史会话的时间计算正确

- **当** 用户查看会话列表
- **那么** 所有会话的时间显示必须基于正确的时区计算
