## 1. CSS 变量系统更新

- [x] 1.1 更新 `frontend/src/index.css` 中的圆角变量（增大到 16-28px 范围）
- [x] 1.2 简化 `index.css` 中的背景样式，移除浮动光球相关动画
- [x] 1.3 更新 `index.css` 中的玻璃效果变量，调整为浅色通透风格
- [x] 1.4 更新间距变量，增加 20-30% 的间距

## 2. 布局组件样式修改

- [x] 2.1 修改 `AppLayout.tsx`，移除浮动光球装饰元素
- [x] 2.2 简化 `AppLayout.tsx` 的背景为柔和渐变
- [x] 2.3 修改 `Sidebar.tsx` 为浅色通透风格
- [x] 2.4 更新 `Sidebar.tsx` 中的 Logo 区域样式

## 3. 聊天组件样式修改

- [x] 3.1 更新 `ChatInput.tsx` 的圆角（增大到 24px）
- [x] 3.2 调整 `ChatInput.tsx` 的聚焦样式，简化阴影效果
- [x] 3.3 更新 `MessageBubble.tsx` 的圆角（增大到 20-24px）
- [x] 3.4 简化 `MessageBubble.tsx` 的渐变背景
- [x] 3.5 更新 `MessageList.tsx` 的间距和空状态样式

## 4. 会话组件样式修改

- [x] 4.1 更新 `SessionList.tsx` 适配浅色风格
- [x] 4.2 增大 `SessionList.tsx` 中会话项的圆角
- [x] 4.3 更新 `SessionList.tsx` 中新建按钮的样式

## 5. 验证和测试

- [x] 5.1 启动前端开发服务器，验证所有页面样式正常
- [x] 5.2 验证响应式布局在不同屏幕尺寸下的表现
- [x] 5.3 验证所有交互动画（悬停、点击）正常工作
