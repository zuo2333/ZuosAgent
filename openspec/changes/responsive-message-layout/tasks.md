## 1. 修改消息列表布局

- [x] 1.1 在 `MessageList.tsx` 中将 `max-w-3xl mx-auto` 改为 `w-full px-6`

## 2. 修改输入框布局

- [x] 2.1 在 `ChatInput.tsx` 中将 `max-w-3xl mx-auto` 改为 `w-full px-6`

## 3. 修改 ChatPanel 布局

- [x] 3.1 在 `ChatPanel.tsx` 中将 `max-w-4xl mx-auto` 移除

## 4. 验证布局效果

- [x] 4.1 验证 AI 消息靠左对齐
- [x] 4.2 验证用户消息靠右对齐
- [x] 4.3 验证输入框与消息列表边距一致

## 5. 用户消息气泡样式优化

- [x] 5.1 将用户气泡宽度从 max-w-[90%] 改为自适应内容
- [x] 5.2 将用户气泡颜色从黑色改为浅灰色
- [x] 5.3 减小头像与气泡间距 (gap-4 → gap-2)
- [x] 5.4 气泡内文字居中显示
