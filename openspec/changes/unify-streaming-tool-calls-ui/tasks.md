## 1. 样式常量统一

- [x] 1.1 从 ToolCallCard.tsx 复制 TOOL_ICONS（SVG 组件）、TOOL_NAMES、STATUS_CONFIG 常量到 StreamingToolCalls.tsx
- [x] 1.2 更新 STATUS_CONFIG 以支持运行中状态（添加旋转动画配置）

## 2. StreamingToolCallCard 组件重构

- [x] 2.1 将卡片容器样式改为白色背景 + 浅灰边框，与 ToolCallCard 一致
- [x] 2.2 将 Emoji 图标替换为 SVG 图标（复用 ToolCallCard 的图标组件）
- [x] 2.3 重构状态徽章为小圆点 + 文字标签样式
- [x] 2.4 运行中状态徽章使用蓝色圆点 + "Running"，与 success 风格一致
- [x] 2.5 将参数展示从 JSON.stringify 改为行内 key="value" 格式

## 3. 运行状态优化

- [x] 3.1 移除假进度条（固定 60% 宽度的进度条）
- [x] 3.2 保留耗时计时器，优化显示样式

## 4. 折叠/展开行为

- [x] 4.1 运行中的工具默认展开显示详情
- [x] 4.2 已完成的工具默认折叠，只显示标题和状态

## 5. 状态转换动画

- [x] 5.1 为状态徽章添加颜色过渡动画（背景色、文字色、圆点色）
- [x] 5.2 设置过渡时长 200ms，缓动函数使用项目 CSS 变量

## 6. 验证

- [ ] 6.1 测试单个工具调用显示
- [ ] 6.2 测试多个工具调用堆叠显示
- [ ] 6.3 验证运行中 → 完成状态切换的视觉一致性
- [ ] 6.4 验证状态转换动画效果
